endpoints:
  - method: GET
    path: "/api/lighten/finenurse/v1/patient"
    todo: true
    name: 환자 목록 가져오기
    description: |
      장치 목록을 가져옵니다.
    request:
      requestHeaders: 
      requestParams:
      requestBody: 
    response:
      success: |
      fail: |

  - method: POST
    path: "/api/lighten/finenurse/v1/patient"
    name: 환자 등록하기
    todo: true
    description:
    request:
      requestHeaders: 
      requestParams:
      requestBody: 
    response:
      success:
      fail:

  - method: PUT
    path: "/api/lighten/finenurse/v1/patient"
    name: 환자 등록하기
    todo: true
    description:
    request:
      requestHeaders: 
      requestParams:
      requestBody: 
    response:
      success:
      fail:

  - method: DELETE
    path: "/api/lighten/finenurse/v1/patient"
    name: 환자 등록하기
    todo: true
    description:
    request:
      requestHeaders: 
      requestParams:
      requestBody: 
    response:
      success:
      fail:
