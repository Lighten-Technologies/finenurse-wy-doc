endpoints:
  - method: GET
    path: "/api/lighten/finenurse/v1/pairing"
    todo: true
    name: 페어링 목록 가져오기
    description: |
      페어링 목록을 가져옵니다.
    request:
      requestHeaders:
      requestParams:
      requestBody:
    response:
      success: |
      fail: |

  - method: POST
    path: "/api/lighten/finenurse/v1/pairing"
    name: 페어링 등록하기
    todo: true
    description:
    request:
      requestHeaders:
      requestParams:
      requestBody:
    response:
      success:
      fail:

  - method: PUT
    path: "/api/lighten/finenurse/v1/pairing"
    name: 페어링 등록하기
    todo: true
    description:
    request:
      requestHeaders:
      requestParams:
      requestBody:
    response:
      success:
      fail:

  - method: DELETE
    path: "/api/lighten/finenurse/v1/pairing"
    name: 페어링 등록하기
    todo: true
    description:
    request:
      requestHeaders:
      requestParams:
      requestBody:
    response:
      success:
      fail:
